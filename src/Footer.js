import React, { } from 'react';

import './Footer.css';

const Footer = (props) => {
	return (
		<div className="Footer">
			<h4 className="monospace underline">hello@kimispencer.com</h4>
		</div>
	);
}

export default Footer;